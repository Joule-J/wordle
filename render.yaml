services:
  - type: web
    name: wordle-chat-backend
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: CLIENT_ORIGIN
        value: "*"
